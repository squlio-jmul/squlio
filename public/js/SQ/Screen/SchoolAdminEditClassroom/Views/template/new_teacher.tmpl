<div class="row new-teacher-container new-teacher-container-<%= classroom_teacher.id %>">
	<div class="col-xs-3">
		<div class="teacher-name">
			<%= classroom_teacher.teacher.first_name + ' ' + classroom_teacher.teacher.last_name %>
		</div>
	</div>
	<div class="col-xs-9">
		<button class="button red remove-teacher" data-classroom-teacher-id="<%= classroom_teacher.id %>">Remove</button>
		<button class="button set-primary set-primary-<%= classroom_teacher.id %> <%= (classroom_teacher.is_primary) ? 'hidden':'' %>" data-classroom-teacher-id="<%= classroom_teacher.id %>">Set Primary</button>
		<span class="is-primary is-primary-<%= classroom_teacher.id %> <%= (classroom_teacher.is_primary) ? '':'hidden' %>" data-classroom-teacher-id="<%= classroom_teacher.id %>">Primary</button>
	</div>
</div>
